services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: petclinic-app
    # 8888 is kept for direct debugging of the app container.
    ports:
      - "8888:8080"
    networks:
      - miniproject-net

  jenkins:
    build:
      context: ./jenkins
    container_name: jenkins
    user: root
    ports:
      - "8081:8080"
    environment:
      SONAR_TOKEN: ${SONAR_TOKEN}
    volumes:
      - jenkins_home:/var/jenkins_home
      - zap_reports:/zap/reports
      - /var/run/docker.sock:/var/run/docker.sock
      - ./jenkins/ssh/vagrant_private_key:/run/secrets/vagrant_private_key:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - miniproject-net

  sonarqube:
    build:
      context: ./sonarqube
    container_name: sonarqube
    ports:
      - "9000:9000"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    networks:
      - miniproject-net

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/targets:/etc/prometheus/targets
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - miniproject-net

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    networks:
      - miniproject-net

  zap:
    build:
      context: ./zap/
    container_name: zap
    volumes:
      - zap_reports:/zap/reports
    ports:
      - "8090:8090"
    networks:
      - miniproject-net

  # --- Continuous passive monitoring (ZAP proxy daemon) ---
  # Long-running ZAP in daemon/proxy mode. Any traffic routed through its proxy
  # port is PASSIVELY scanned. It never launches attacks, so it is safe to keep
  # running against the live production deployment.
  zap-monitor:
    build:
      context: ./zap/
    container_name: zap-monitor
    entrypoint:
      - zap.sh
      - -daemon
      - -host
      - 0.0.0.0
      - -port
      - "8090"
      - -config
      - api.disablekey=true
      - -config
      - api.addrs.addr.name=.*
      - -config
      - api.addrs.addr.regex=true
    volumes:
      - zap_reports:/zap/reports
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      # Internal proxy/API port for direct access to ZAP.
      - "8091:8090"
    networks:
      - miniproject-net
    restart: unless-stopped

  # Sidecar loop: every 30s it regenerates the passive-scan HTML report snapshot
  # in the shared zap_reports volume from whatever traffic the ZAP proxy has
  # observed. It generates NO traffic of its own.
  zap-reporter:
    image: curlimages/curl:latest
    container_name: zap-reporter
    depends_on:
      - zap-monitor
    volumes:
      - zap_reports:/zap/reports
    entrypoint: ["/bin/sh", "-c"]
    command: |
      # Wait for the ZAP proxy/API to come up.
      until curl -s -o /dev/null http://zap-monitor:8090/; do sleep 2; done
      while true; do
        # Regenerate the report from real traffic the passive scanner has seen.
        curl -s "http://zap-monitor:8090/OTHER/core/other/htmlreport/" -o /zap/reports/latest-monitor.html || true
        sleep 30
      done
    restart: unless-stopped
    networks:
      - miniproject-net

networks:
  miniproject-net:
    name: miniproject-net

volumes:
  jenkins_home:
  grafana_data:
  zap_reports:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_logs:
